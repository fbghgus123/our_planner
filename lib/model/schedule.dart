class Schedule {
  int id;
  String title;
  String content;
  String color;
  DateTime date;
  DateTime startTime;
  DateTime endTime;
  bool check;
  Schedule({id, title, content, color, date, startTime, endTime, check})
}
